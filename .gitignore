# --- Dependencies ---
node_modules/
.pnp
.pnp.js
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# --- IDEs / Editors ---
.vscode/
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.DS_Store
Thumbs.db

# --- Build & Output ---
/public/assets/css/style.css
.netlify/
logs/
dist/
build/

# --- Tools & Reports ---
report_*.json
*:Zone.Identifier
*.csv
lighthouse.*
.lighthouse/
# Lighthouse (WSL) may drop stray dirs into the repo root
undefined:/
\\wsl.localhost/
undefined:*
# Windows-style paths leaked by a crashed Lighthouse run
C:\\Users\\*
.gemini/
coverage/
*.log
consultoriosalustiano.com.br.report.json
*ANALISE_*.md

# --- Assets ---
# Only ignore raw/original assets if they are strictly source files
# src/assets/logo/
# Local agent learnings (machine-specific operational notes)
AGENTS.md

# Secrets
.env
.env.*
!*.env.example
*credentials*.json

# Google Ads CLI tooling — local-only, never versioned (code, data, credentials)
ads/

# Weekly metrics report scratch output (local run of `npm run weekly-report --out`)
.weekly-report/

# Monthly product report scratch output (local run of `npm run product-report --out`)
.product-report/
